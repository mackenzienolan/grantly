---
const { width = "100%", height = "1px", color = "#ccc", margin = "1rem 0" } = Astro.props;
---
<hr style={`width: ${width}; height: ${height}; background-color: ${color}; margin: ${margin}; border: none;`} />